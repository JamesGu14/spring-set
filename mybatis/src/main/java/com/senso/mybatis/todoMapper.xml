<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.senso.mybatis.todoMapper">
<!-- 
	<select id="selectTodos" resultType="java.util.List">
	 select * from Todos order by id desc
	</select> -->
	
	<select id="selectTodo" resultType="com.senso.mybatis.Todo">
		select * from Todos where id = #{id}
	</select>
	
	
</mapper>